This utility helps determine the required ArduinoJson doc size in Arduino code to avoid overflows.

The JSON files in this directory are examples. They contain the structure of a JSON doc filled with the maximum theoretical values to calculate the maximum theoretical size of such an ArduinoJson doc using the Python script.
